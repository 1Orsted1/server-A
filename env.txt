NODE_ENV=development


#server config

PORT=*Tu puerto*
HOST=*Cambiar por el host(puede ser localhost)*
HOST_URL=http://*lo mismo que en host*:*el mismo pueto que seleccionaste*
API_VERSION=v1
COOKIE_ENCRYPT_PWD=*Un string con al menos 30 caracteres de largo*


#sqlserver config

SQL_USER=*Su usuario*
SQL_PASSWORD=*Su contraseña*
SQL_DATABASE=*La base de datos(como sea que la nombro)*
SQL_SERVER=*server de la base de datos*
SQL_ENCRYPT=false


